# ──────────────────────────────────────────────────────────────────────────────
# Dynamic runtime files (generated, never hand-edited)
# ──────────────────────────────────────────────────────────────────────────────
qs_colors.json
colors.json
settings.json
scripts/quickshell/calendar/.env

# Autogenerated by scripts/compiler.lua from lua/ sources.
# Edit lua/config/*.lua instead — the .conf files are regenerated on every save.
config/autostart.conf
config/env.conf
config/keybindings.conf
config/monitors.conf
config/rules.conf
config/settings.conf
config/variables.conf
# ──────────────────────────────────────────────────────────────────────────────
# NOTE: colors.conf / hypridle.conf are intentionally tracked —
# they are hand-maintained source files, not generated output.
# ──────────────────────────────────────────────────────────────────────────────
hyprland.conf