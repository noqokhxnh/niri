# ──────────────────────────────────────────────────────────────────────────────
# Dynamic runtime files (generated, never hand-edited)
# ──────────────────────────────────────────────────────────────────────────────
qs_colors.json
colors.json
settings.json
settings.json.lock
bin/quickshell/calendar/.env


# Autogenerated by lua.lua from lua/ sources.
# Edit lua/config/*.lua instead — the .conf files are regenerated on every save.
config/autostart.conf
config/env.conf
config/keybindings.conf
config/monitors.conf
config/rules.conf
config/settings.conf
config/variables.conf
# ──────────────────────────────────────────────────────────────────────────────
# NOTE: colors.conf is intentionally tracked —
# it is a hand-maintained source file, not generated output.
# ──────────────────────────────────────────────────────────────────────────────
hyprland.conf
*.bak
**/.codewhale/*
!**/.codewhale/constitution.json
.deepseek/
.opencode/*
.mimocode/*